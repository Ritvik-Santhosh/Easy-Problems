#include<iostream>
using namespace std;    
int main()
{
      int n;
cout<<"Enter the Number: ";
    cin>>n;
     if(n>0)
   cout<<"The no is positive: "<<n;
     else if(n<0)
   cout<<"The no is Negative: "<<n;
   else
   cout<<"The no is Zero: "<<n;
    return 0;
}